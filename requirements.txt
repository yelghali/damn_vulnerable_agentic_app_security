# ---------------------------------------------------------------------------
# Zava Wealth Advisor — Damn Vulnerable Agentic App
# Pinned, minimal dependency set. See docs/workshop.md Module 0 for setup.
# ---------------------------------------------------------------------------

# --- Multi-agent orchestration (the APP) ---
agent-framework>=1.0.0b251001            # Microsoft Agent Framework (multi-agent, middleware, HITL)

# --- Azure AI Foundry project SDK (project, models, agents, evals) ---
azure-ai-projects>=2.0.0
azure-identity>=1.19.0

# --- Local SLM backend for offline mode (real model, no Azure) ---
openai>=1.54.0                            # OpenAI-compatible client (Foundry Local / Ollama / Azure)
foundry-local-sdk>=0.1.0                  # Microsoft Foundry Local: run a real SLM locally, free

# --- Azure AI security building blocks ---
azure-ai-contentsafety>=1.0.0            # Content Safety + Prompt Shields (V1/V2)
azure-ai-textanalytics>=5.3.0            # Azure AI Language PII detection/redaction (V3)
azure-search-documents>=11.6.0           # RAG + document-level security trimming (V5/V6)
azure-keyvault-secrets>=4.8.0            # Key Vault secret access (V5)

# --- Assurance (Extended track) ---
azure-ai-evaluation>=1.0.0               # Evaluations + AI Red Teaming Agent (M9/M10)

# --- Backend / web ---
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
jinja2>=3.1.0
python-multipart>=0.0.12

# --- Data layer ---
psycopg[binary]>=3.2.0                    # PostgreSQL Flexible Server tools (V4)

# --- Config / utilities ---
pydantic>=2.9.0
pydantic-settings>=2.6.0
python-dotenv>=1.0.0
httpx>=0.27.0

# --- Dev / test (local validation, no Azure required) ---
pytest>=8.3.0
pytest-asyncio>=0.24.0
ruff>=0.7.0
